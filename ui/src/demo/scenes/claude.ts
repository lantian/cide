import type { Scene } from '../scenes'

/**
 * The hero: the pinned Claude tab's 2×2 grid — three conversations and a shell — beside the file
 * tree, which is the base world as `world.ts` builds it. Nothing to add.
 */
export const claude: Scene = {
  setup: () => {},
}
