//! Fuzzy pickers (files, commands, symbols) behind a `Matcher` trait, so `nucleo` staying unreleased since 2024 is a swap and not a rewrite. (M8)
//!
//! M0 stub: the crate exists so the workspace graph and the module boundaries are real
//! from the first commit. Implementation lands in the milestone noted above.
