// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { GitLabDocument } from "./GitLabDocument";
import type { GitLabPreferences } from "./GitLabPreferences";
import type { GitLabSeverity } from "./GitLabSeverity";

export type GitLabRequest = { "kind": "board" } | { "kind": "comparison", document: GitLabDocument, } | { "kind": "connect", host: string, token: string, } | { "kind": "disconnect", account: string, } | { "kind": "preferences", preferences: GitLabPreferences, } | { "kind": "list", account: string, scope: string, state: string, search: string, project: string | null, page: number, } | { "kind": "open", account: string, url: string, } | { "kind": "close", review: string, } | { "kind": "detail", review: string, } | { "kind": "diffs", review: string, version: number, page: number, } | { "kind": "versions", review: string, } | { "kind": "notes", review: string, page: number, } | { "kind": "discussions", review: string, page: number, } | { "kind": "comment", review: string, body: string, discussion: string | null, position: unknown, } | { "kind": "resolve", review: string, discussion: string, resolved: boolean, } | { "kind": "approve", review: string, sha: string, undo: boolean, } | { "kind": "approvals", review: string, } | { "kind": "pipelines", review: string, page: number, } | { "kind": "commits", review: string, page: number, } | { "kind": "jobs", review: string, project: number, pipeline: number, page: number, bridges: boolean, } | { "kind": "trace", review: string, project: number, job: number, } | { "kind": "file", review: string, project: number, path: string, sha: string, } | { "kind": "checkout", review: string, sha: string, } | { "kind": "sourceTree", review: string, sha: string, } | { "kind": "sourceFile", review: string, sha: string, path: string, } | { "kind": "afterPush", account: string, remoteUrl: string, branch: string, } | { "kind": "drafts", review: string, } | { "kind": "draftEdit", review: string, draft: string, body: string | null, severity: GitLabSeverity | null, } | { "kind": "draftDiscard", review: string, drafts: Array<string>, } | { "kind": "draftPublish", review: string, drafts: Array<string>, };
