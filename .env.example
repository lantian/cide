# Personal launch environment — sourced by run.sh (allexport), gitignored.
# Missing file = empty file; comment a line out to fall back to the ladder.

# The fork overrides: run cide's own builds instead of the bundled/PATH ones.
CIDE_RA_PATH=<path to rust-analyzer folder>
CIDE_GOPLS_PATH=<path to tools/gopls/cide-gopls folder>
